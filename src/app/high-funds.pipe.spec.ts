import { HighFundsPipe } from './high-funds.pipe';

describe('HighFundsPipe', () => {
  it('create an instance', () => {
    const pipe = new HighFundsPipe();
    expect(pipe).toBeTruthy();
  });
});
